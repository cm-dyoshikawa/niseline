{
  "name": "client-sdk",
  "version": "0.0.0",
  "private": false,
  "main": "dist/index.js",
  "dependencies": {
    "@line/liff": "2.18.1",
    "sleep-promise": "9.1.0"
  },
  "scripts": {
    "build": "esbuild src/index.ts --minify --sourcemap --target=chrome58,firefox57,safari11,edge16 --outfile=dist/index.js && tsc --emitDeclarationOnly --outDir dist",
    "lint:js": "eslint '**/*.{js,ts}'",
    "lint:js:fix": "eslint --fix '**/*.{js,ts}'",
    "typec": "tsc --noEmit"
  },
  "author": "dyoshikawa",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.11"
  }
}
